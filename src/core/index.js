import { generateMap, getRandomSpawn, getMultipleSpawns, getSpawnFromRoom} from './mapgen';

const GameGen = {
    generateMap,
    getRandomSpawn,
    getSpawnFromRoom,
    getMultipleSpawns,
};

export default GameGen;